<h2 class="mb-4">Categorias</h2>
<hr>
{{#each categorias}}
    <div class="card mb-4">
        <div class="card-body">
            <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
        </div>
    </div>
    {{else}}
    <h5>Nenhuma categoria registrada</h5>
{{/each}}