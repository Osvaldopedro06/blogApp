{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
    {{else}}

{{/each}}
<h3>Atualizar Postagem</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/postagens/edit" method="post">
            <input type="hidden" name="txtId" value="{{postagem._id}}">
            <label for="titulo">Título:</label>
            <input type="text" name="txtTitulo" id="titulo" placeholder="Título da postagem" value="{{postagem.titulo}}" class="form-control mb-3" required>
            <label for="slug">Slug:</label>
            <input type="text" name="txtSlug" id="slug" placeholder="Slug da postagem" value="{{postagem.slug}}" class="form-control mb-3" required>
            <label for="descricao">Descrição:</label>
            <input type="text" name="txtDescricao" id="descricao" placeholder="Descrição da postagem" value="{{postagem.descricao}}" class="form-control mb-3" required>
            <label for="conteudo">Conteúdo:</label>
            <textarea name="txtConteudo" id="conteudo" placeholder="Conteúdo da postagem" class="form-control mb-3" required>{{postagem.conteudo}}</textarea>
            <label for="categoria">Categoria:</label><br>
            <select name="txtCategoria" id="categoria" class="form-select">
                {{#each categorias}}
                    <option value="{{_id}}">{{nome}}</option>
                    {{else}}
                    <option value="0">Nenhuma categoria registrada</option>
                {{/each}}
            </select>
            <button type="submit" class="btn btn-success mt-3">Atualizar Postagem</button>
        </form>
    </div>
</div>