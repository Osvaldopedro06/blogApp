<div class="jumbotrom">
    <h1 class="display-4">Bem-vindo ao Blog de TI!</h1>
    <p class="lead">Este é um blog desenvolvido com Node.js + Express + MongoDB</p>
    <hr class="my-4">
    <p>Esteja a par de tudo o que acontece no mundo das tecnologias</p>
    <p class="lead">
        <a href="#" class="btn btn-primary btn-lg" role="button">Crie uma conta</a>
    </p>
</div>
    <hr class=" mt-4 mb-4">
    <h2 class="mb-4">Postagens mais recentes</h2>
{{#each postagens}}
    <div class="card mb-4">
        <div class="card-body">
            <h4 class="mb-4">{{titulo}}</h4>
            <p>{{descricao}}</p>
            <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
            <hr>
            <small>Data da postagem: {{data}}</small><br>
            <small>Categoria: {{categoria.nome}}</small><br>
        </div>
    </div>
    {{else}}
    <h5>Nenhuma postagem foi feita</h5>
{{/each}}